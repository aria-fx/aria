# Changelog

All notable changes to aria CLI are documented in this file.

The format follows Keep a Changelog categories and SemVer intent.

## Unreleased

### Added
- `aria scaffold` command for installing curated preset bundles via `--preset`, with `--skills-only` to skip agent assets. First preset: `usage-eval` (alias `provider-usage-evaluator`) — 4 usage-evaluation MCP skills @ 1.0.1 plus the `provider-usage-evaluator` agent @ 1.0.0. Each asset runs the standard governance-enforced install flow; failures are summarized per asset and produce a non-zero exit code.

## 0.0.1-22 - 2026-05-03

### Changed
- Generated by publish workflow. Replace with user-facing release notes.
### Added
-

## 0.1.0 - 2026-05-03

### Added
- Initial ARIA CLI commands for asset discovery, inspection, governance audit, and install workflows.
- Cross-target installation adapters for Claude Desktop, VS Code MCP, and Agent Framework.
- NuGet tool package and npm wrapper package for distribution through GitHub Packages.
